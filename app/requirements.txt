# Dependências mínimas do backend (scaffolding inicial).
# backend-engineer expande conforme os módulos forem implementados
# (ex.: httpx, resend).
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic-settings==2.6.1
asyncpg==0.30.0
structlog==24.4.0
sentry-sdk[fastapi]==2.17.0

# Client Supabase (assíncrono) — usado por app/db/supabase_client.py e
# app/db/auth.py (dependency de autenticação) e pelos repositories dos
# módulos de domínio (RLS via token do usuário, nunca service_role).
supabase==2.10.0
